CREATE TABLE Grid (
  id TEXT NOT NULL PRIMARY KEY ,
  startTime INTEGER NOT NULL,
  endTime INTEGER,
  grid TEXT NOT NULL
);

CREATE INDEX Grid_id ON Grid(id);

insertGrid:
INSERT OR REPLACE INTO Grid (id, startTime,endTime, grid)
VALUES (?,?,?,?);

getGridForId:
SELECT *
FROM Grid
WHERE id = ?;