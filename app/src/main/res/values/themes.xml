<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.MinesweeperJC" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/damask</item>
        <item name="colorOnPrimary">@color/beech</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/tuscany</item>
        <item name="colorOnSecondary">@color/beech</item>

        <!-- Background color. -->
        <item name="backgroundColor">@color/navajo</item>
        <item name="background">@color/navajo</item>
        <item name="colorOnBackground">@color/beech</item>

        <item name="android:windowBackground">?attr/backgroundColor</item>

        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

    </style>

</resources>